const fs = require('fs'); let content = fs.readFileSync('src/app/[locale]/HomePageClient.tsx', 'utf8'); const oldLine = content.split('\n').find(l => l.includes('shop?seasons')); if (oldLine) { const modifiedLine = oldLine.replace(/seasons=\\$\\{[^}]+\\}/, 'seasons='); content = content.replace(oldLine, modifiedLine); fs.writeFileSync('src/app/[locale]/HomePageClient.tsx', content); console.log('Fixed'); } else { console.log('Not found'); }
