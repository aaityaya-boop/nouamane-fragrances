const fs = require('fs'); let content = fs.readFileSync('src/app/[locale]/HomePageClient.tsx', 'utf8'); const str = 'seasons='; const start = content.indexOf('shop?seasons='); if(start > -1){ const end = content.indexOf('}', start); content = content.substring(0, start+5) + str + content.substring(end+1); fs.writeFileSync('src/app/[locale]/HomePageClient.tsx', content); console.log('Fixed'); } else { console.log('Not found'); }
